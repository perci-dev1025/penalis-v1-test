PORT=3003
# PostgreSQL. Neon: use URL from dashboard; sslmode and connect_timeout are added for neon.tech.
# If you get "Can't reach database server", wake the project at https://console.neon.tech or try the Direct (non-pooler) connection string.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/penalis?schema=public"
JWT_SECRET=<generate-a-long-random-secret>
CORS_ORIGIN=http://localhost:5173
